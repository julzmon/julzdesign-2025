<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- https://github.com/joshbuchea/HEAD -->

    <!-- Recommended -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Meta -->
    <meta name="description" content="{{ metadata.description }}" />
    <meta name="generator" content="{{ eleventy.generator }}" />

    <!-- Icons -->
    <!-- https://favicon.io/ -->
    <link
      rel="apple-touch-icon"
      sizes="180x180"
      href="{{ '/apple-touch-icon.png' | url }}"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="32x32"
      href="{{ '/favicon-32x32.png' | url }}"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="16x16"
      href="{{ '/favicon-16x16.png' | url }}"
    />
    <link rel="manifest" href="{{ '/site.webmanifest' | url }}" />

    <!-- Google Fonts: Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet">

    <title>{{ title or metadata.title }}</title>

  <link rel="stylesheet" href="{{ '/styles/site.css' | url }}" />
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        // Password protection code ...
        var protectedProjects = [
          { selector: 'a[href$="/wtr/"]', key: 'wtr-auth', password: 'enter' }
        ];
        protectedProjects.forEach(function(project) {
          var link = document.querySelector(project.selector);
          if (link) {
            link.addEventListener('click', function(e) {
              if (localStorage.getItem(project.key) === 'true') return;
              e.preventDefault();
              var password = prompt('This project is password protected. Please enter the password to continue:');
              if (password === project.password) {
                localStorage.setItem(project.key, 'true');
                window.location.href = link.href;
              } else if (password !== null) {
                alert('Incorrect password. Access denied.');
              }
            });
          }
        });

        // Header hide/show on scroll
        const header = document.getElementById('main-header');
        let lastScrollY = window.scrollY;
        let lastHideScrollY = window.scrollY; // where we last hid the header
        let ticking = false;
        // Tuning knobs
        const HIDE_OFFSET = 150;      // pixels from top before we start hiding on scroll down
        const SHOW_THRESHOLD = 200;   // pixels scrolled up from last hide before showing again

        function updateHeader() {
          const currentScroll = window.scrollY;
          const scrollingDown = currentScroll > lastScrollY;
          const scrollingUp = currentScroll < lastScrollY;

          // Always show near the top
          if (currentScroll <= HIDE_OFFSET) {
            header.style.translate = '-50% 0';
            header.style.opacity = '1';
          } else if (scrollingDown) {
            // Hide when scrolling down past the offset
            header.style.translate = '-50% -120%';
            header.style.opacity = '0';
            lastHideScrollY = currentScroll; // remember where we hid
          } else if (scrollingUp) {
            // Only show after the user scrolls up a minimum distance from the last hide point
            const scrolledUpBy = lastHideScrollY - currentScroll;
            if (scrolledUpBy >= SHOW_THRESHOLD) {
              header.style.translate = '-50% 0';
              header.style.opacity = '1';
            }
          }
          lastScrollY = currentScroll;
          ticking = false;
        }

        window.addEventListener('scroll', function() {
          if (!ticking) {
            window.requestAnimationFrame(updateHeader);
            ticking = true;
          }
        });
      });
    </script>
  </head>

  <body>
    <header id="main-header" class="p-2 bg-black text-white fixed top-6 left-1/2 -translate-x-1/2 mx-auto rounded-full shadow-lg backdrop-blur z-50 w-fit transition-all duration-500">
      <nav>
        <ul class="flex gap-1 justify-center">
          <li><a class="h-10 inline-flex place-items-center rounded-full py-2 px-3 {% if page.url === '/' %} bg-slate-600 text-white{% endif %}" href="{{ '/' | url }}"><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-house-door-fill" viewBox="0 0 16 16"><path d="M6.5 14.5v-3.505c0-.245.25-.495.5-.495h2c.25 0 .5.25.5.5v3.5a.5.5 0 0 0 .5.5h4a.5.5 0 0 0 .5-.5v-7a.5.5 0 0 0 -.146-.354L13 5.793V2.5a.5.5 0 0 0 -.5-.5h-1a.5.5 0 0 0 -.5.5v1.293L8.354 1.146a.5.5 0 0 0 -.708 0l-6 6A.5.5 0 0 0 1.5 7.5v7a.5.5 0 0 0 .5.5h4a.5.5 0 0 0 .5-.5"/></svg> <span class="sr-only">Home</span></a></li>
          {# <li><a class="h-10 inline-flex place-items-center rounded-full py-2 px-3 {% if page.url === '/wtr/' %} bg-slate-600 text-white{% endif %}" href="{{ '/wtr/' | url }}">KeyBank</a></li> #}
          <li><a class="h-10 inline-flex place-items-center rounded-full py-2 px-3 {% if page.url === '/wtr/' %} bg-slate-600 text-white{% endif %}" href="{{ '/wtr/' | url }}">WestRock</a></li>
          <li><a class="h-10 inline-flex place-items-center rounded-full py-2 px-3 {% if page.url === '/pru/' %} bg-slate-600 text-white{% endif %}" href="{{ '/pru/' | url }}">Prudential</a></li>
          {# <li class="mr-3"><a class="h-10 inline-flex place-items-center rounded py-1 px-3 {% if page.url === '/ui/' %} bg-slate-600 text-white{% endif %}" href="{{ '/ui/' | url }}">UI Design</a></li> #}
        </ul>
      </nav>
    </header>
  <div class="gradient-wrap">
      <div class="grid grid-flow-col auto-cols-fr mb-12">
        <div class="flex justify-center pt-[7rem] pb-5">j</div>
        <div class="border-l border-gray-300 flex justify-center pt-[7rem] pb-5">u</div>
        <div class="border-l border-gray-300 flex justify-center pt-[7rem] pb-5">l</div>
        <div class="border-l border-gray-300 flex justify-center pt-[7rem] pb-5">z</div>
        <div class="border-l border-gray-300 flex justify-center pt-[7rem] pb-5">d</div>
        <div class="border-l border-gray-300 flex justify-center pt-[7rem] pb-5">e</div>
        <div class="border-l border-gray-300 flex justify-center pt-[7rem] pb-5">s</div>
        <div class="border-l border-gray-300 flex justify-center pt-[7rem] pb-5">i</div>
        <div class="border-l border-gray-300 flex justify-center pt-[7rem] pb-5">g</div>
        <div class="border-l border-gray-300 flex justify-center pt-[7rem] pb-5">n</div>
      </div>
      <main class="max-w-page mx-auto">
        {{ content | safe }}
      </main>
    </div>
  </body>
</html>
